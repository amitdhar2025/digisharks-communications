# TODO - Mobile nav cyan highlight (CSS-only)

- [ ] Inspect `src/app/(public)/home.css` mobile menu selectors for `.mobile-menu` hover/active behavior.
- [ ] Add smallest CSS-only change to make mobile/touch devices show cyan highlight on menu items without breaking desktop `:hover`.
- [ ] Ensure `.active` and existing hover rules remain unchanged for desktop.
- [ ] Validate no unintended side effects by grepping for `.mobile-menu a:hover` / `.mobile-menu a.active` occurrences.

